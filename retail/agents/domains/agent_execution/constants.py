"""Module-level constants for the agent_execution domain.

Centralises plain-string contract values shared across the buffer and
the high-level logger so the wire values stay in one place and can't
drift via typos at the call sites.
"""

UNKNOWN_CONTACT_URN = "unknown"
LEGACY_FLOW_AGENT_LABEL = "legacy_flow"
